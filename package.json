{
  "name": "mufettis-sosyal-tesis",
  "version": "2.0.0",
  "description": "İstanbul sosyal tesisleri için etkileşimli Web GIS + karar destek (PostgreSQL + PostGIS)",
  "private": true,
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "test": "node backend/test-db.js && node backend/test-orders.js && node backend/test-analytics.js && node backend/test-concurrency.js && node backend/test-routes.js && node backend/test-admin.js && node backend/test-auth.js",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:load-geo": "node scripts/load-geo.js",
    "db:generate": "node scripts/generate-data.js",
    "export:analytics": "node scripts/export-analytics.js",
    "export:schema": "node scripts/export-schema.js",
    "build:routes": "node scripts/build-routes.js",
    "check": "node scripts/check-consistency.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=22"
  }
}
